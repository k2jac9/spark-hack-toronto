# Generated audio artifacts (ElevenLabs TTS/SFX/music) — large binaries, not committed.
# The narration *source* lives in ../NARRATION.md (committed); regenerate audio from it.
*.mp3
*.wav
*.flac
*.ogg
*.m4a
# keep this folder + its docs in git
!.gitignore
!README.md
!MANIFEST.md
